schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: aws
  nativeType: AWS::EC2::VPCEndpointService
  class: network.endpoint_service
presentation:
  displayNames:
    zh-CN: VPC 终端节点服务
    en-US: VPC Endpoint Service
scope:
  kind: region
discovery:
  source: cloud-control
  list:
    operation: com.amazonaws.cloudcontrol#ListResources
    parameters:
      TypeName: AWS::EC2::VPCEndpointService
    itemsPath: ResourceDescriptions
    identityPath: Identifier
    pagination:
      type: token
      tokenParameter: NextToken
      tokenPath: NextToken
      pageSizeParameter: MaxResults
      maxPageSize: 100
  detail:
    operation: com.amazonaws.cloudcontrol#GetResource
    itemsPath: ResourceDescription
    identityPath: Identifier
relationships:
- type: uses
  targetType: AWS::ElasticLoadBalancingV2::LoadBalancer
  targetIdPath: NetworkLoadBalancerArns
- type: uses
  targetType: AWS::ElasticLoadBalancingV2::LoadBalancer
  targetIdPath: GatewayLoadBalancerArns
actions:
  delete:
    operation: com.amazonaws.cloudcontrol#DeleteResource
    idempotency: provider_token
    waiter: cloud_control_terminal
    readback: cloud_control_absent
extensions:
  hook: aws.cloudcontrol.resource
