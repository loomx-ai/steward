schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: gcp
  nativeType: discoveryengine.googleapis.com/TargetSite
  class: search.site
presentation:
  displayNames:
    en-US: Discovery Engine TargetSite
    zh-CN: Discovery Engine 目标网站
  consoleLinkTemplate: https://console.cloud.google.com/gen-app-builder/engines?project={projectId}
scope:
  kind: region
discovery:
  source: product-api
  list:
    operation: discoveryengine.projects.locations.collections.dataStores.siteSearchEngine.targetSites.list
    parameters:
      parent: parent.nativeId
    itemsPath: targetSites
    identityPath: name
    pagination:
      type: token
      tokenParameter: pageToken
      tokenPath: nextPageToken
      pageSizeParameter: pageSize
      maxPageSize: 100
  detail: &id001
    operation: discoveryengine.projects.locations.collections.dataStores.siteSearchEngine.targetSites.get
    itemsPath: $
    identityPath: name
  parent:
    source: product-api
    nativeType: discoveryengine.googleapis.com/SiteSearchEngine
fields:
  name:
    path: name
    type: string
  projectId:
    path: project_id
    type: string
  displayName:
    path: displayName
    type: string
  createTime:
    path: createTime
    type: string
  state:
    path: state
    type: string
extensions:
  hook: gcp.resource
relationships:
- type: depends_on
  targetType: discoveryengine.googleapis.com/SiteSearchEngine
  targetIdPath: refs_discoveryengine_googleapis_com_SiteSearchEngine
actions:
  delete:
    operation: discoveryengine.projects.locations.collections.dataStores.siteSearchEngine.targetSites.delete
    idempotency: readback
    waiter: gcp_operation
    readback: gcp_absent
    read: *id001
