schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: gcp
  nativeType: compute.googleapis.com/GlobalForwardingRule
  class: network.load_balancer
presentation:
  displayNames:
    en-US: Global forwarding rule
    zh-CN: 全局转发规则
  consoleLinkTemplate: https://console.cloud.google.com/home/dashboard?project={projectId}
scope:
  kind: global
discovery:
  source: cloud-asset-inventory
  detail:
    operation: compute.globalForwardingRules.get
    itemsPath: $
    identityPath: name
fields:
  name:
    path: name
    type: string
  state:
    path: status
    type: string
  projectId:
    path: project_id
    type: string
  zoneId:
    path: zone_id
    type: string
  labels:
    path: labels
    type: object
extensions:
  hook: gcp.resource
relationships:
- type: uses
  targetType: compute.googleapis.com/BackendService
  targetIdPath: refs_compute_googleapis_com_BackendService
- type: uses
  targetType: compute.googleapis.com/TargetHttpProxy
  targetIdPath: refs_compute_googleapis_com_TargetHttpProxy
- type: uses
  targetType: compute.googleapis.com/TargetHttpsProxy
  targetIdPath: refs_compute_googleapis_com_TargetHttpsProxy
actions:
  delete:
    operation: compute.globalForwardingRules.delete
    idempotency: readback
    waiter: gcp_operation
    readback: gcp_absent
    read:
      operation: compute.globalForwardingRules.get
      itemsPath: $
      identityPath: name
