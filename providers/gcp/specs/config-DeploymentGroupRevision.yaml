schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: gcp
  nativeType: config.googleapis.com/DeploymentGroupRevision
  class: orchestration.revision
presentation:
  displayNames:
    en-US: Infrastructure Manager deployment group revision
    zh-CN: Infrastructure Manager 部署组修订
  consoleLinkTemplate: https://console.cloud.google.com/infra-manager/deployments?project={projectId}
scope:
  kind: region
discovery:
  source: product-api
  parent:
    nativeType: config.googleapis.com/DeploymentGroup
    source: product-api
  list:
    operation: config.projects.locations.deploymentGroups.revisions.list
    parameters:
      parent: parent.nativeId
    itemsPath: deploymentGroupRevisions
    identityPath: name
    pagination:
      type: token
      tokenParameter: pageToken
      tokenPath: nextPageToken
      pageSizeParameter: pageSize
      maxPageSize: 1000
  detail: &read
    operation: config.projects.locations.deploymentGroups.revisions.get
    itemsPath: $
    identityPath: name
fields:
  name:
    path: name
    type: string
  projectId:
    path: project_id
    type: string
  state:
    path: state
    type: string
extensions:
  hook: gcp.resource
relationships:
- type: depends_on
  targetType: config.googleapis.com/DeploymentGroup
  targetIdPath: refs_config_googleapis_com_DeploymentGroup
