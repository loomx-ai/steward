schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: gcp
  nativeType: config.googleapis.com/Resource
  class: orchestration.managed_resource
presentation:
  displayNames:
    en-US: Infrastructure Manager resource record
    zh-CN: Infrastructure Manager 资源记录
  consoleLinkTemplate: https://console.cloud.google.com/infra-manager/deployments?project={projectId}
scope:
  kind: region
discovery:
  source: product-api
  parent:
    nativeType: config.googleapis.com/Revision
    source: product-api
  list:
    operation: config.projects.locations.deployments.revisions.resources.list
    parameters:
      parent: parent.nativeId
    itemsPath: resources
    identityPath: name
    pagination:
      type: token
      tokenParameter: pageToken
      tokenPath: nextPageToken
      pageSizeParameter: pageSize
      maxPageSize: 1000
  detail: &read
    operation: config.projects.locations.deployments.revisions.resources.get
    itemsPath: $
    identityPath: name
fields:
  name:
    path: name
    type: string
  projectId:
    path: project_id
    type: string
  state:
    path: state
    type: string
extensions:
  hook: gcp.resource
relationships:
- type: depends_on
  targetType: config.googleapis.com/Revision
  targetIdPath: refs_config_googleapis_com_Revision
