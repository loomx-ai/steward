schema: steward.io/resource-kind
kind: ResourceKind
metadata:
  provider: alicloud
  nativeType: ACS::CDN::Domain
  class: network.cdn_domain
presentation:
  consoleLinkTemplate: "https://cdn.console.aliyun.com/domain/detail/{nativeId}"
  displayNames:
    zh-CN: CDN 加速域名
    en-US: CDN Domain
  icon: dns
scope:
  kind: global
discovery:
  source: product-api
  list:
    operation: AlibabaCloud.CDN.DescribeUserDomains
    itemsPath: Domains.PageData
    identityPath: DomainName
    statePath: DomainStatus
    pagination:
      type: page-number
      pageParameter: PageNumber
      pageSizeParameter: PageSize
      totalPath: TotalCount
      maxPageSize: 500
fields:
  name: DomainName
  state: DomainStatus
  createdAt: GmtCreated
  updatedAt: GmtModified
  resourceGroupId: ResourceGroupId
  cname: Cname
actions:
  delete:
    operation: AlibabaCloud.CDN.DeleteCdnDomain
    parameters:
      DomainName: resource.nativeId
    idempotency: readback
    waiter: absent
    read:
      operation: AlibabaCloud.CDN.DescribeUserDomains
      parameters:
        DomainName: resource.nativeId
        DomainSearchType: full_match
        PageSize: 1
      itemsPath: Domains.PageData
      identityPath: DomainName
      statePath: DomainStatus
